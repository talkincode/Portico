{
  "lock": false,
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "fmt": {
    "lineWidth": 100,
    "exclude": ["**/*.md"]
  },
  "tasks": {
    "test": "deno test --allow-read --allow-write --allow-env --allow-run tests/",
    "check": "deno check src/cli/main.ts src/catalog/mod.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "cli": "deno run --allow-read --allow-write --allow-env src/cli/main.ts"
  },
  "exclude": ["data", "tmp"]
}
