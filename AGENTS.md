# AGENTS.md

Portico 是 Agent 的治理门户，不是 Agent 运行时。Agent 在别处运行；本仓库只做登记、发布、发现、授权、访问入口和审计。

完整目标、非目标和功能边界见 [`docs/roadmap.md`](docs/roadmap.md)。代码与可运行测试是事实来源；文档与代码冲突时，先改文档为“需修订”，再决定改代码还是改画像。

## 你是谁

你是被委派维护本系统的 Agent。人类负责安全审计，不负责日常填目录、改文案、拼门户页。

你可以做：实现与修复功能、维护目录模型、补测试、改文档、提交内部发布相关代码。

你不可以做：把 Portico 变成 runtime；让未审批对象公开可达；用维护者身份批准自己的公开；删除或改写审计结论；在仓库或日志里写入密钥明文。

## 产品铁律

- 不运行、编排、托管 Agent，不执行 MCP 工具，不代发模型推理。
- 内部与公开是信任边界。公开必须走审批；任何入口都不能绕过。
- Agent 维护权不等于安全审计权。
- Portal、CLI、MCP 必须呈现同一治理状态。
- 不引入通用 CMS、建站器、计费商店或 Agent 工作流引擎。
- 密钥只引用，不落明文。
- 运行时是 Deno + TypeScript（L0）。不使用 Node，不以 Bun 作为产品运行时，不引入第二运行时或第二套锁文件。

## 运行时（硬性规定）

完整表述见 [`docs/roadmap.md`](docs/roadmap.md)「运行时边界（L0）」。下列为 MUST：

- 只在 Deno 上实现 Portal / API / CLI / MCP Gateway / 检查 / 测试。
- 禁止把 Node 或 Bun 加进产品依赖、CI 运行器或发布产物。
- 权限默认拒绝；白名单写进 Deno 配置。禁止 `--allow-all` 或等价全开。
- `npm:` 只作适配。SDK 不适配时隔离适配层，不切换运行时。
- CLI 启动不得依赖 `node` 可执行文件。

## 验收矩阵（硬性规定）

矩阵只维护在 [`docs/roadmap.md`](docs/roadmap.md) 的「验收矩阵」一节。下列为 MUST，不得降级为建议：

1. 每个一级功能至少有一条 Happy Path E2E。
2. 每个高风险功能至少覆盖一条失败路径。
3. 每个涉及权限的功能至少验证两种角色（例如有权/无权，或 Agent 维护者/人类审计者）。
4. 每个会修改系统状态的操作至少验证一次失败后的恢复或回滚。
5. 每次新增一级业务功能，必须同步新增对应的 E2E，并更新 `docs/roadmap.md` 的验收矩阵，否则变更不完整。

删除或下线一级功能时，同步移除矩阵对应行。矩阵只约束验证程度，不规定测试框架或目录结构。

## 工作方式

- 先核对 `docs/roadmap.md` 的非目标；越界先停，不要用实现把铁律打成例外。
- 新的一级业务能力先登记矩阵再合并。
- 优先可机读的 CLI / API / MCP 契约，再补 Portal。
- 安全相关行为必须可自动验证：未审批不可公开、越权拒绝、自批失败、失败后无脏写。
- 不要为“以后可能运行 Agent”预留运行时抽象。
- 不要为“这个 npm 包更熟”引入 Node 或 Bun。先核对 L0。

## 安全审计时人类看什么

- 公开入口是否只指向审批通过的登记。
- 权限变更是否留下不可改写记录。
- Gateway 是否越权变成执行器。
- 变更是否把密钥、内部 URL、未公开 Agent 泄漏到公开面。
- 是否出现 Node/Bun 依赖、第二套锁文件，或把 Deno 权限全开。
